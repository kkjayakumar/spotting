{
  "name": "@spotting/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "check-types": "tsc -p tsconfig.json --noEmit",
    "test": "bun test src",
    "profile": "bun run src/profile.ts"
  },
  "dependencies": {
    "@spotting/config": "workspace:*",
    "@prisma/client": "^6.6.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4"
  }
}
